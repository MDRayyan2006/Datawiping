# Requirements for building DataWipe with PyInstaller
# Install with: pip install -r requirements-build.txt

# Core application dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic[email]==2.5.0
python-multipart==0.0.6
psutil==5.9.6
cryptography==41.0.7
reportlab==4.0.4

# Build tools
pyinstaller>=5.13.0

# Optional: For creating Windows installers
# pywin32>=306  # Windows only
# pywin32-ctypes>=0.2.0  # Windows only

# Optional: For creating Linux installers
# fpm  # Linux only (install via package manager)

# Development and testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
